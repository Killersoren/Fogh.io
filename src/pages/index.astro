---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title="BALD SOFTWARE GUY" description="The most epic bald software developer you'll ever meet" />
		<style>
			@import url('https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&family=Space+Mono:wght@400;700&display=swap');
			
			:root {
				--neon-blue: #00ffff;
				--neon-pink: #ff00ff;
				--neon-green: #00ff00;
				--dark-bg: #0a0a0a;
				--matrix-green: #00ff41;
				--cyber-purple: #8a2be2;
				--gold: #ffd700;
				--orange: #ff6600;
				/* SUPER GAY COLORS */
				--rainbow-red: #ff0000;
				--rainbow-orange: #ff8000;
				--rainbow-yellow: #ffff00;
				--rainbow-green: #00ff00;
				--rainbow-blue: #0080ff;
				--rainbow-purple: #8000ff;
				--rainbow-pink: #ff0080;
			}

			body {
				background: var(--dark-bg);
				color: white;
				font-family: 'Space Mono', monospace;
				margin: 0;
				overflow-x: hidden;
			}

			/* SUPER GAY DICK CONFETTI */
			.dick-confetti-container {
				position: fixed;
				top: 0;
				left: 0;
				width: 100%;
				height: 100%;
				pointer-events: none;
				z-index: 100;
				overflow: hidden;
			}

			.dick-confetti {
				position: absolute;
				font-size: 1.5rem;
				animation: dickFall 8s linear infinite;
				opacity: 0.8;
			}

			.dick-confetti:nth-child(odd) {
				animation-duration: 6s;
				color: var(--rainbow-pink);
			}

			.dick-confetti:nth-child(even) {
				animation-duration: 7s;
				color: var(--rainbow-blue);
			}

			.dick-confetti:nth-child(3n) {
				animation-duration: 9s;
				color: var(--rainbow-green);
			}

			.dick-confetti:nth-child(4n) {
				animation-duration: 5s;
				color: var(--rainbow-yellow);
			}

			.dick-confetti:nth-child(5n) {
				animation-duration: 10s;
				color: var(--rainbow-purple);
			}

			/* RAINBOW PARTICLE BACKGROUND */
			.particle-container {
				position: fixed;
				top: 0;
				left: 0;
				width: 100%;
				height: 100%;
				z-index: 1;
				pointer-events: none;
			}

			.particle {
				position: absolute;
				width: 4px;
				height: 4px;
				border-radius: 50%;
				animation: particleFloat 8s linear infinite;
			}

			.particle:nth-child(6n+1) { background: var(--rainbow-red); animation-duration: 6s; }
			.particle:nth-child(6n+2) { background: var(--rainbow-orange); animation-duration: 7s; }
			.particle:nth-child(6n+3) { background: var(--rainbow-yellow); animation-duration: 8s; }
			.particle:nth-child(6n+4) { background: var(--rainbow-green); animation-duration: 9s; }
			.particle:nth-child(6n+5) { background: var(--rainbow-blue); animation-duration: 10s; }
			.particle:nth-child(6n+6) { background: var(--rainbow-purple); animation-duration: 11s; }

			/* RAINBOW SCANNING LINES */
			.scan-line {
				position: fixed;
				top: 0;
				left: 0;
				width: 100%;
				height: 2px;
				z-index: 2;
				animation: scanLine 3s linear infinite;
			}

			.scan-line:nth-child(1) {
				background: linear-gradient(90deg, transparent, var(--rainbow-red), transparent);
			}

			.scan-line:nth-child(2) {
				animation-delay: 1s;
				background: linear-gradient(90deg, transparent, var(--rainbow-orange), transparent);
			}

			.scan-line:nth-child(3) {
				animation-delay: 2s;
				background: linear-gradient(90deg, transparent, var(--rainbow-yellow), transparent);
			}

			.scan-line:nth-child(4) {
				animation-delay: 3s;
				background: linear-gradient(90deg, transparent, var(--rainbow-green), transparent);
			}

			.scan-line:nth-child(5) {
				animation-delay: 4s;
				background: linear-gradient(90deg, transparent, var(--rainbow-blue), transparent);
			}

			.scan-line:nth-child(6) {
				animation-delay: 5s;
				background: linear-gradient(90deg, transparent, var(--rainbow-purple), transparent);
			}

			.hero-section {
				min-height: 100vh;
				display: flex;
				align-items: center;
				justify-content: center;
				position: relative;
				background: linear-gradient(45deg, #000000, #1a1a1a, #000000);
				overflow: hidden;
				z-index: 10;
			}

			/* RAINBOW MATRIX BACKGROUND */
			.matrix-bg {
				position: absolute;
				top: 0;
				left: 0;
				width: 100%;
				height: 100%;
				opacity: 0.3;
				z-index: 1;
			}

			.matrix-column {
				position: absolute;
				top: -100%;
				font-size: 1.2rem;
				font-family: 'Space Mono', monospace;
				animation: matrixRain 8s linear infinite;
				text-shadow: 0 0 10px currentColor;
			}

			.matrix-column:nth-child(6n+1) { color: var(--rainbow-red); }
			.matrix-column:nth-child(6n+2) { color: var(--rainbow-orange); }
			.matrix-column:nth-child(6n+3) { color: var(--rainbow-yellow); }
			.matrix-column:nth-child(6n+4) { color: var(--rainbow-green); }
			.matrix-column:nth-child(6n+5) { color: var(--rainbow-blue); }
			.matrix-column:nth-child(6n+6) { color: var(--rainbow-purple); }

			/* RAINBOW FLASHING BORDER EFFECT */
			.hero-section::before {
				content: '';
				position: absolute;
				top: 0;
				left: 0;
				right: 0;
				bottom: 0;
				border: 3px solid transparent;
				background: linear-gradient(45deg, var(--rainbow-red), var(--rainbow-orange), var(--rainbow-yellow), var(--rainbow-green), var(--rainbow-blue), var(--rainbow-purple)) border-box;
				-webkit-mask: linear-gradient(#fff 0 0) padding-box, linear-gradient(#fff 0 0);
				-webkit-mask-composite: destination-out;
				mask: linear-gradient(#fff 0 0) padding-box, linear-gradient(#fff 0 0);
				mask-composite: exclude;
				animation: rainbowBorderRotate 4s linear infinite;
			}

			.hero-content {
				text-align: center;
				z-index: 20;
				position: relative;
			}

			.bald-guy-title {
				font-family: 'Orbitron', monospace;
				font-size: 5rem;
				font-weight: 900;
				background: linear-gradient(45deg, var(--rainbow-red), var(--rainbow-orange), var(--rainbow-yellow), var(--rainbow-green), var(--rainbow-blue), var(--rainbow-purple));
				background-size: 300% 300%;
				-webkit-background-clip: text;
				-webkit-text-fill-color: transparent;
				background-clip: text;
				animation: neonGlow 2s ease-in-out infinite, titleFloat 4s ease-in-out infinite, titlePulse 1s ease-in-out infinite;
				text-shadow: 
					0 0 20px var(--rainbow-pink),
					0 0 40px var(--rainbow-pink),
					0 0 60px var(--rainbow-pink),
					0 0 80px var(--rainbow-pink);
				margin-bottom: 2rem;
				position: relative;
			}

			.bald-guy-title::before {
				content: 'BALD SOFTWARE GUY';
				position: absolute;
				top: 0;
				left: 0;
				width: 100%;
				height: 100%;
				background: linear-gradient(45deg, var(--rainbow-purple), var(--rainbow-blue), var(--rainbow-green));
				background-size: 300% 300%;
				-webkit-background-clip: text;
				-webkit-text-fill-color: transparent;
				background-clip: text;
				animation: neonGlow 2s ease-in-out infinite reverse;
				filter: blur(3px);
				z-index: -1;
			}

			.subtitle {
				font-size: 1.8rem;
				color: var(--rainbow-yellow);
				margin-bottom: 3rem;
				animation: pulse 1.5s ease-in-out infinite, glowText 3s ease-in-out infinite;
				text-shadow: 0 0 15px var(--rainbow-yellow);
			}

			.bald-avatar {
				width: 250px;
				height: 250px;
				border-radius: 50%;
				background: linear-gradient(45deg, var(--rainbow-red), var(--rainbow-orange), var(--rainbow-yellow), var(--rainbow-green), var(--rainbow-blue), var(--rainbow-purple));
				background-size: 400% 400%;
				margin: 0 auto 2rem;
				position: relative;
				animation: rotate 15s linear infinite, glow 1s ease-in-out infinite alternate, avatarPulse 2s ease-in-out infinite;
				display: flex;
				align-items: center;
				justify-content: center;
				font-size: 5rem;
				box-shadow: 
					0 0 30px var(--rainbow-pink),
					0 0 60px var(--rainbow-blue),
					0 0 90px var(--rainbow-green);
			}

			.bald-avatar::before {
				content: '';
				position: absolute;
				top: -10px;
				left: -10px;
				right: -10px;
				bottom: -10px;
				border-radius: 50%;
				background: linear-gradient(45deg, var(--rainbow-red), var(--rainbow-orange), var(--rainbow-yellow), var(--rainbow-green), var(--rainbow-blue), var(--rainbow-purple));
				background-size: 400% 400%;
				animation: rotate 10s linear infinite reverse, glow 1.5s ease-in-out infinite alternate;
				z-index: -1;
				filter: blur(10px);
			}

			.tech-stack {
				display: flex;
				justify-content: center;
				gap: 2rem;
				flex-wrap: wrap;
				margin: 3rem 0;
			}

			.tech-item {
				padding: 1.5rem 2.5rem;
				background: rgba(255, 0, 255, 0.1);
				border: 3px solid var(--rainbow-pink);
				border-radius: 15px;
				color: var(--rainbow-pink);
				font-weight: bold;
				font-size: 1.1rem;
				animation: techFloat 3s ease-in-out infinite, techGlow 2s ease-in-out infinite;
				cursor: pointer;
				transition: all 0.3s ease;
				position: relative;
				overflow: hidden;
				text-shadow: 0 0 10px var(--rainbow-pink);
			}

			.tech-item::before {
				content: '';
				position: absolute;
				top: 0;
				left: -100%;
				width: 100%;
				height: 100%;
				background: linear-gradient(90deg, transparent, rgba(255, 0, 255, 0.5), transparent);
				transition: left 0.5s ease;
			}

			.tech-item:hover {
				background: var(--rainbow-pink);
				color: var(--dark-bg);
				transform: scale(1.2) rotate(5deg);
				box-shadow: 
					0 0 30px var(--rainbow-pink),
					0 0 60px var(--rainbow-pink);
				border-color: var(--rainbow-blue);
			}

			.tech-item:hover::before {
				left: 100%;
			}

			.cta-button {
				padding: 1.5rem 4rem;
				font-size: 1.4rem;
				font-weight: bold;
				background: linear-gradient(45deg, var(--rainbow-red), var(--rainbow-orange), var(--rainbow-yellow), var(--rainbow-green), var(--rainbow-blue), var(--rainbow-purple));
				background-size: 200% 200%;
				border: none;
				border-radius: 50px;
				color: white;
				cursor: pointer;
				animation: buttonPulse 1.5s ease-in-out infinite, buttonGlow 2s ease-in-out infinite;
				transition: all 0.3s ease;
				text-transform: uppercase;
				letter-spacing: 3px;
				position: relative;
				overflow: hidden;
				box-shadow: 
					0 0 20px var(--rainbow-pink),
					0 0 40px var(--rainbow-blue);
			}

			.cta-button::before {
				content: '';
				position: absolute;
				top: 0;
				left: -100%;
				width: 100%;
				height: 100%;
				background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
				transition: left 0.5s ease;
			}

			.cta-button:hover {
				transform: scale(1.1);
				box-shadow: 
					0 0 40px var(--rainbow-pink),
					0 0 80px var(--rainbow-blue),
					0 0 120px var(--rainbow-green);
			}

			.cta-button:hover::before {
				left: 100%;
			}

			.floating-elements {
				position: absolute;
				width: 100%;
				height: 100%;
				pointer-events: none;
				z-index: 5;
			}

			.floating-element {
				position: absolute;
				font-size: 3rem;
				animation: float 6s ease-in-out infinite;
				opacity: 0.4;
				text-shadow: 0 0 15px currentColor;
			}

			.floating-element:nth-child(6n+1) { color: var(--rainbow-red); }
			.floating-element:nth-child(6n+2) { color: var(--rainbow-orange); }
			.floating-element:nth-child(6n+3) { color: var(--rainbow-yellow); }
			.floating-element:nth-child(6n+4) { color: var(--rainbow-green); }
			.floating-element:nth-child(6n+5) { color: var(--rainbow-blue); }
			.floating-element:nth-child(6n+6) { color: var(--rainbow-purple); }

			/* SUPER GAY ANIMATIONS */
			@keyframes dickFall {
				0% { 
					transform: translateY(-100vh) translateX(0px) rotate(0deg); 
					opacity: 0; 
				}
				10% { opacity: 1; }
				90% { opacity: 1; }
				100% { 
					transform: translateY(100vh) translateX(100px) rotate(360deg); 
					opacity: 0; 
				}
			}

			@keyframes neonGlow {
				0%, 100% { background-position: 0% 50%; }
				50% { background-position: 100% 50%; }
			}

			@keyframes titleFloat {
				0%, 100% { transform: translateY(0px) rotate(0deg); }
				50% { transform: translateY(-15px) rotate(1deg); }
			}

			@keyframes titlePulse {
				0%, 100% { transform: scale(1); }
				50% { transform: scale(1.05); }
			}

			@keyframes pulse {
				0%, 100% { opacity: 1; transform: scale(1); }
				50% { opacity: 0.8; transform: scale(1.02); }
			}

			@keyframes glowText {
				0%, 100% { text-shadow: 0 0 15px var(--rainbow-yellow); }
				50% { text-shadow: 0 0 30px var(--rainbow-yellow), 0 0 45px var(--rainbow-yellow); }
			}

			@keyframes rotate {
				from { transform: rotate(0deg); }
				to { transform: rotate(360deg); }
			}

			@keyframes glow {
				from { 
					box-shadow: 
						0 0 20px var(--rainbow-pink),
						0 0 40px var(--rainbow-blue);
				}
				to { 
					box-shadow: 
						0 0 40px var(--rainbow-blue),
						0 0 60px var(--rainbow-green),
						0 0 80px var(--rainbow-yellow);
				}
			}

			@keyframes avatarPulse {
				0%, 100% { transform: scale(1); }
				50% { transform: scale(1.1); }
			}

			@keyframes techFloat {
				0%, 100% { transform: translateY(0px) rotate(0deg); }
				50% { transform: translateY(-8px) rotate(2deg); }
			}

			@keyframes techGlow {
				0%, 100% { box-shadow: 0 0 10px var(--rainbow-pink); }
				50% { box-shadow: 0 0 20px var(--rainbow-pink), 0 0 30px var(--rainbow-pink); }
			}

			@keyframes buttonPulse {
				0%, 100% { transform: scale(1); }
				50% { transform: scale(1.05); }
			}

			@keyframes buttonGlow {
				0%, 100% { 
					box-shadow: 
						0 0 20px var(--rainbow-pink),
						0 0 40px var(--rainbow-blue);
				}
				50% { 
					box-shadow: 
						0 0 40px var(--rainbow-pink),
						0 0 80px var(--rainbow-blue),
						0 0 120px var(--rainbow-green);
				}
			}

			@keyframes float {
				0%, 100% { transform: translateY(0px) rotate(0deg) scale(1); }
				50% { transform: translateY(-25px) rotate(180deg) scale(1.2); }
			}

			@keyframes particleFloat {
				0% { transform: translateY(100vh) translateX(0px); opacity: 0; }
				10% { opacity: 1; }
				90% { opacity: 1; }
				100% { transform: translateY(-100px) translateX(100px); opacity: 0; }
			}

			@keyframes scanLine {
				0% { transform: translateY(-100vh); }
				100% { transform: translateY(100vh); }
			}

			@keyframes matrixRain {
				0% { transform: translateY(-100vh); }
				100% { transform: translateY(100vh); }
			}

			@keyframes rainbowBorderRotate {
				0% { background: linear-gradient(0deg, var(--rainbow-red), var(--rainbow-orange), var(--rainbow-yellow), var(--rainbow-green), var(--rainbow-blue), var(--rainbow-purple)); }
				16.66% { background: linear-gradient(60deg, var(--rainbow-orange), var(--rainbow-yellow), var(--rainbow-green), var(--rainbow-blue), var(--rainbow-purple), var(--rainbow-red)); }
				33.33% { background: linear-gradient(120deg, var(--rainbow-yellow), var(--rainbow-green), var(--rainbow-blue), var(--rainbow-purple), var(--rainbow-red), var(--rainbow-orange)); }
				50% { background: linear-gradient(180deg, var(--rainbow-green), var(--rainbow-blue), var(--rainbow-purple), var(--rainbow-red), var(--rainbow-orange), var(--rainbow-yellow)); }
				66.66% { background: linear-gradient(240deg, var(--rainbow-blue), var(--rainbow-purple), var(--rainbow-red), var(--rainbow-orange), var(--rainbow-yellow), var(--rainbow-green)); }
				83.33% { background: linear-gradient(300deg, var(--rainbow-purple), var(--rainbow-red), var(--rainbow-orange), var(--rainbow-yellow), var(--rainbow-green), var(--rainbow-blue)); }
				100% { background: linear-gradient(360deg, var(--rainbow-red), var(--rainbow-orange), var(--rainbow-yellow), var(--rainbow-green), var(--rainbow-blue), var(--rainbow-purple)); }
			}

			@media (max-width: 768px) {
				.bald-guy-title {
					font-size: 3rem;
				}
				.subtitle {
					font-size: 1.3rem;
				}
				.bald-avatar {
					width: 180px;
					height: 180px;
					font-size: 3.5rem;
				}
				.tech-stack {
					gap: 1rem;
				}
				.tech-item {
					padding: 1rem 1.5rem;
					font-size: 1rem;
				}
			}
		</style>
	</head>
	<body>
		<Header />
		
		<!-- SUPER GAY DICK CONFETTI -->
		<div class="dick-confetti-container" id="dickConfetti"></div>
		
		<!-- RAINBOW PARTICLE BACKGROUND -->
		<div class="particle-container" id="particles"></div>
		
		<!-- RAINBOW SCANNING LINES -->
		<div class="scan-line"></div>
		<div class="scan-line"></div>
		<div class="scan-line"></div>
		<div class="scan-line"></div>
		<div class="scan-line"></div>
		<div class="scan-line"></div>
		
		<section class="hero-section">
			<div class="matrix-bg" id="matrixBg">
				<!-- Matrix rain effect will be added via JavaScript -->
			</div>
			
			<div class="floating-elements">
				<div class="floating-element" style="top: 10%; left: 10%; animation-delay: 0s;">{ }</div>
				<div class="floating-element" style="top: 20%; right: 15%; animation-delay: 1s;">&lt;/&gt;</div>
				<div class="floating-element" style="bottom: 30%; left: 20%; animation-delay: 2s;">()</div>
				<div class="floating-element" style="bottom: 20%; right: 10%; animation-delay: 3s;">[]</div>
				<div class="floating-element" style="top: 50%; left: 5%; animation-delay: 4s;">//</div>
				<div class="floating-element" style="top: 60%; right: 5%; animation-delay: 5s;">&&</div>
				<div class="floating-element" style="top: 30%; left: 50%; animation-delay: 6s;">++</div>
				<div class="floating-element" style="bottom: 40%; left: 30%; animation-delay: 7s;">==</div>
			</div>

			<div class="hero-content">
				<div class="bald-avatar">👨‍🦲</div>
				<h1 class="bald-guy-title">BALD SOFTWARE GUY</h1>
				<p class="subtitle">Coding with style, debugging with grace, and absolutely owning the bald aesthetic</p>
				
				<div class="tech-stack">
					<div class="tech-item">JavaScript</div>
					<div class="tech-item">TypeScript</div>
					<div class="tech-item">React</div>
					<div class="tech-item">Node.js</div>
					<div class="tech-item">Python</div>
					<div class="tech-item">AWS</div>
				</div>
				
				<button class="cta-button" onclick="scrollToAbout()">Explore My Code</button>
			</div>
		</section>

		<main>
			<section id="about" style="padding: 4rem 0; background: linear-gradient(135deg, #1a1a1a, #2a2a2a);">
				<div style="max-width: 1200px; margin: 0 auto; padding: 0 2rem;">
					<h2 style="text-align: center; font-family: 'Orbitron', monospace; font-size: 3rem; color: var(--rainbow-pink); margin-bottom: 3rem; text-shadow: 0 0 20px var(--rainbow-pink);">
						WHO IS THIS BALD LEGEND?
					</h2>
					
					<div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 3rem; margin-top: 3rem;">
						<div style="background: rgba(255, 0, 255, 0.1); padding: 2rem; border-radius: 15px; border: 2px solid var(--rainbow-pink);">
							<h3 style="color: var(--rainbow-pink); font-family: 'Orbitron', monospace; margin-bottom: 1rem;">🚀 Full-Stack Developer</h3>
							<p>Building web applications that are as smooth as my head. From frontend to backend, I make it all work seamlessly.</p>
						</div>
						
						<div style="background: rgba(0, 255, 255, 0.1); padding: 2rem; border-radius: 15px; border: 2px solid var(--rainbow-blue);">
							<h3 style="color: var(--rainbow-blue); font-family: 'Orbitron', monospace; margin-bottom: 1rem;">🎯 Problem Solver</h3>
							<p>Debugging is my superpower. I find bugs faster than you can say "it works on my machine."</p>
						</div>
						
						<div style="background: rgba(0, 255, 0, 0.1); padding: 2rem; border-radius: 15px; border: 2px solid var(--rainbow-green);">
							<h3 style="color: var(--rainbow-green); font-family: 'Orbitron', monospace; margin-bottom: 1rem;">💡 Innovation Enthusiast</h3>
							<p>Always exploring new technologies and pushing the boundaries of what's possible in software development.</p>
						</div>
					</div>
				</div>
			</section>
		</main>

		<Footer />

		<script>
			// SUPER GAY DICK CONFETTI SYSTEM
			function createDickConfetti() {
				const container = document.getElementById('dickConfetti');
				const dicks = ['🍆', '🥕', '🌽', '🥖', '🍌', '🥒'];
				
				for (let i = 0; i < 50; i++) {
					const dick = document.createElement('div');
					dick.className = 'dick-confetti';
					dick.textContent = dicks[Math.floor(Math.random() * dicks.length)];
					dick.style.left = Math.random() * 100 + '%';
					dick.style.animationDelay = Math.random() * 8 + 's';
					dick.style.animationDuration = (5 + Math.random() * 5) + 's';
					dick.style.fontSize = (1 + Math.random() * 1.5) + 'rem';
					container.appendChild(dick);
				}
			}

			// RAINBOW PARTICLE SYSTEM
			function createParticles() {
				const container = document.getElementById('particles');
				for (let i = 0; i < 100; i++) {
					const particle = document.createElement('div');
					particle.className = 'particle';
					particle.style.left = Math.random() * 100 + '%';
					particle.style.animationDelay = Math.random() * 8 + 's';
					particle.style.animationDuration = (6 + Math.random() * 4) + 's';
					container.appendChild(particle);
				}
			}

			// RAINBOW MATRIX RAIN
			function createMatrixRain() {
				const matrixBg = document.getElementById('matrixBg');
				const characters = '01アイウエオカキクケコサシスセソタチツテトナニヌネノハヒフヘホマミムメモヤユヨラリルレロワヲン{}[]()<>//&&++==🌈🍆✨💖';
				
				for (let i = 0; i < 30; i++) {
					const column = document.createElement('div');
					column.className = 'matrix-column';
					column.style.left = (i * 3.33) + '%';
					column.style.animationDelay = Math.random() * 8 + 's';
					column.style.animationDuration = (6 + Math.random() * 4) + 's';
					
					let columnText = '';
					for (let j = 0; j < 20; j++) {
						columnText += characters[Math.floor(Math.random() * characters.length)] + '<br>';
					}
					column.innerHTML = columnText;
					matrixBg.appendChild(column);
				}
			}

			// Smooth scroll function
			function scrollToAbout() {
				document.getElementById('about').scrollIntoView({
					behavior: 'smooth'
				});
			}

			// Initialize effects when page loads
			document.addEventListener('DOMContentLoaded', function() {
				createDickConfetti();
				createParticles();
				createMatrixRain();
				
				// Add some interactive effects
				const techItems = document.querySelectorAll('.tech-item');
				techItems.forEach(item => {
					item.addEventListener('mouseenter', function() {
						this.style.animation = 'none';
						setTimeout(() => {
							this.style.animation = 'techFloat 3s ease-in-out infinite, techGlow 2s ease-in-out infinite';
						}, 10);
					});
				});

				// Add click effects to title
				const title = document.querySelector('.bald-guy-title');
				title.addEventListener('click', function() {
					this.style.animation = 'titlePulse 0.5s ease-in-out';
					setTimeout(() => {
						this.style.animation = 'neonGlow 2s ease-in-out infinite, titleFloat 4s ease-in-out infinite, titlePulse 1s ease-in-out infinite';
					}, 500);
				});
			});
		</script>
	</body>
</html>
